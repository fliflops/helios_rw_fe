export type userTableType = {
    id: string;
    email: string;
    is_active: boolean;
    is_lock: boolean;
    is_reset: boolean;
    first_name:string;
    last_name: string;
    role_name: string;
}